@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
find_dependency(OpenSSL 3 REQUIRED)
if(@PROTOCOL_ENABLE_CONNECTION@)
	find_dependency(ZLIB REQUIRED)
	find_dependency(Snappy REQUIRED)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/ProtocolTargets.cmake")

check_required_components(Protocol)